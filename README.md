vim-setup
=========

Installation:

        cd ~
        git clone git@github.com:lonelycode/vim-setup.git .vim
        cd ~/.vim
        git submodule init
        git submodule update
        cp .vimrc ~/.vimrc
